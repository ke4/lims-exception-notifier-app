source "http://rubygems.org"

# Specify your gem's dependencies in lims-exception-notifier-app.gemspec
gemspec

group :debugger do
  gem 'debugger', :platforms => :mri
  gem 'debugger-completion', :platforms => :mri
end

